import React from "react";

function Header() {
  const name = "BAŞLIK";

  return (
    <div className="test">
      <h1>{name}</h1>
      <label htmlFor="test"></label>
    </div>
  );
}

export default Header;
